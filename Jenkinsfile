pipeline {
    agent any

    stages {

        stage (build source code) {
            steps {
                
                echo "its a stage to build the source code"
            
            }
        }
    }
}
